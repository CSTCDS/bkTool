// assets/js/app.js - small helper
// Service worker registration disabled to prevent offline caching during development.
// If you want SW in production, re-enable registration.
// if ('serviceWorker' in navigator) {
//   navigator.serviceWorker.register('/sw.js').catch(()=>{});
// }
